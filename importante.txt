Tambien como en los desfios anteriores dejo un archivo .env(que se llena con los datos correspondientes) que tiene:
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DATABASE=

PUERTO=3000

Tambien se hacen varios cambios finales en el css de la pagina pero solo visuales.